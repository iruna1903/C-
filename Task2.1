using System;
namespace Lab2_1
{
    class Program
    {
        static void Main()
        {
            float n1, n2, n3;
            n1 = Convert.ToInt32(Console.ReadLine());
            n2 = Convert.ToInt32(Console.ReadLine());
            n3 = Convert.ToInt32(Console.ReadLine());
            string borgs = (n1 >= -5) && (n1 <= 5) ? "Числа в межі (-5:5)" : (n2 >= -5) && (n2 <= 5) ? "Числа в межі (-5:5)" : (n3 >= -5) && (n3 <= 5) ? "Числа в межі (-5:5)" : "Не одна зміна в межі (-5:5)";
            Console.WriteLine(borgs);
        }
    }
}
