using System;
namespace Lab2_1
{
    class Program
    {
        static void Main()
        {
            float n1, n2, n3;
            n1 = Convert.ToInt32(Console.ReadLine());
            n2 = Convert.ToInt32(Console.ReadLine());
            n3 = Convert.ToInt32(Console.ReadLine());
            string check1 = (n1 >= -5) && (n1 <= 5) ? "Перше число належить діапазону" : "Перше число не належить діапазону";
            string check2 = (n2 >= -5) && (n2 <= 5) ? "Друге число належить діапазону" : "Друге число не належить діапазону";
            string check3 = (n3 >= -5) && (n3 <= 5) ? "Третє число належить діапазону" : "Третє число не належить діапазону";
            Console.WriteLine(check1);
            Console.WriteLine(check2);
            Console.WriteLine(check3);

        }
    }
}
